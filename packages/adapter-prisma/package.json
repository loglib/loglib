{
  "name": "@loglib/prisma-adapter",
  "version": "0.2.1",
  "description": "",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "prisma db push && vitest run",
    "test:watch": "vitest watch",
    "build": "prisma generate && tsup",
    "dev": "tsup --watch",
    "db:push": "prisma db push",
    "studio": "prisma studio",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts"
  },
  "files": [
    "dist",
    "package.json",
    "README.md"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@loglib/adapter-test": "1.2.0",
    "@prisma/client": "^4.14.0",
    "prisma": "^4.14.0"
  },
  "dependencies": {
    "@loglib/core": "0.3.1"
  },
  "peerDependencies": {
    "@prisma/client": "^4.14.0",
    "prisma": "^4.14.0"
  }
}
